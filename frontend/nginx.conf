server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Allow health checks at /health if desired
    location = /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}
